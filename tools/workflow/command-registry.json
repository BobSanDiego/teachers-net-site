{
  "version": 1,
  "internal_lifecycle": "Conversation refresh is automatically performed before every queue-related command; it is not a user command.",
  "commands": [
    {"command": "EXECUTE NEXT", "syntax": "EXECUTE NEXT", "purpose": "Refresh conversation, rebuild queue, then execute the oldest formal pending ticket", "stop": "Stop if refresh fails, ambiguity exists, or a ticket boundary is reached", "example": "EXECUTE NEXT"},
    {"command": "EXECUTE", "syntax": "EXECUTE <ticket-id>", "purpose": "Refresh conversation, rebuild queue, then execute one named formal ticket", "stop": "Stop if refresh fails or the ledger rejects the ticket", "example": "EXECUTE DATA002"},
    {"command": "EXECUTE ALL PENDING", "syntax": "EXECUTE ALL PENDING", "purpose": "Refresh conversation, rebuild queue, and run pending tickets sequentially", "stop": "Stop at the first gate, failure, review, or dependency", "example": "EXECUTE ALL PENDING"},
    {"command": "RETRY BLOCKED", "syntax": "RETRY BLOCKED <ticket-id>", "purpose": "Retry after the blocker is resolved", "stop": "Stop if the blocker remains"},
    {"command": "SHOW QUEUE", "syntax": "SHOW QUEUE", "purpose": "Refresh conversation, rebuild, and display the formal ticket queue", "stop": "Inspection only", "example": "SHOW QUEUE"},
    {"command": "SHOW NEXT", "syntax": "SHOW NEXT", "purpose": "Refresh conversation, rebuild, and explain the next ledger-approved ticket", "stop": "Inspection only", "example": "SHOW NEXT"},
    {"command": "SHOW REPORT", "syntax": "SHOW REPORT", "purpose": "List the current Report directory contents only", "stop": "Inspection only", "example": "SHOW REPORT"},
    {"command": "MARK SUPERSEDED", "syntax": "MARK SUPERSEDED <ticket-id> BY <replacement-ticket-id>", "purpose": "Record replacement without deleting history", "stop": "Ledger update only"},
    {"command": "ARCHIVE CURRENT", "syntax": "ARCHIVE CURRENT", "purpose": "Archive current Report and Hopper payloads", "stop": "Archive only"},
    {"command": "REBUILD REPORT", "syntax": "REBUILD REPORT", "purpose": "Regenerate the Report from current evidence", "stop": "Report generation only"},
    {"command": "SHOW HOPPER INDEX", "syntax": "SHOW HOPPER INDEX", "purpose": "List cycle evidence", "stop": "Inspection only"},
    {"command": "WORKFLOW STATUS", "syntax": "WORKFLOW STATUS", "purpose": "Report project, cycle, queue, directories, and ledger health", "stop": "Inspection only"},
    {"command": "VALIDATE WORKFLOW", "syntax": "VALIDATE WORKFLOW", "purpose": "Validate structure, registry, ledger, and artifacts", "stop": "Read-only validation"},
    {"command": "LIST COMMANDS", "syntax": "LIST COMMANDS", "purpose": "Render user commands, ticket markers, and internal lifecycle actions", "stop": "Inspection only", "example": "LIST COMMANDS"}
  ]
}
