TICKET READY FOR CODEX

Ticket: C3-V1-COMMUNITY-MEDIA-DISCOVERY-IMPLEMENTATION001
Mode: STANDARD
Reasoning: MEDIUM
Recommended model: LUNA
Project: Community
Owner: Media QA cleanup and terminal finalization

OUTCOME

Complete the already Director-PASSED Media objective by safely removing its
exact disposable QA fixture, reverting temporary cleanup-policy residue,
consolidating final source/evidence, committing/pushing, and issuing COMPLETE.

DIRECTOR AUTHORITY

Codex is authorized to perform the AWS/IAM work necessary for THIS exact
cleanup without another human permission-by-permission cycle.

Prefer an existing authorized administrative/IaC mechanism over expanding or
repacking TNetC3MediaIaCOperatorBoundary.

Codex owns inspection, exact commands, sequencing, verification and rollback
awareness.

Do not ask the Director to manually execute routine AWS/IAM cleanup steps if an
available authorized mechanism can perform them safely.

EXACT CLEANUP SCOPE

Inventory already proves:

- 30 QA posts matching qa-media-discovery-260915*
- 31 QA post_media relationships
- 3 QA media assets
- 3 QA variant rows
- 12 current ready objects
- no current quarantine objects

Exact media IDs:

media-61e0b7f55feb44e6b26a4e2512bce8a6
media-d3acae146b6a48ebb01835b5d7e8ff95
media-7fc607fa5e054d3da6d9135377a3ac6c

AWS deletion scope is limited to all current objects, historical versions and
delete markers beneath those exact media IDs in the governed C3 media bucket.

No unrelated object/resource deletion is authorized.

PERMISSION STRATEGY

The attempted TNetC3MediaIaCOperatorBoundary expansion is abandoned because
the boundary is at/near its IAM document-size limit.

Do NOT compact, weaken, replace or broaden that boundary merely for cleanup.

Inspect available authorized AWS/IaC/admin mechanisms and use the narrowest
safe existing authority capable of the exact deletion.

If temporary IAM state must be created or changed by an already-authorized
administrative mechanism, it must be:
- bounded to these exact QA prefixes/actions;
- temporary;
- removed and verified absent after cleanup.

Do not alter users, access keys, MFA, unrelated roles/policies, billing,
Organizations, unrelated buckets/resources, or production architecture.

RUNTIME POLICY RESIDUE

The Director already added temporary cleanup statements to the inline:

TNetC3MediaIaCOperatorRuntime

Those statements are ineffective because the matching boundary change was
never saved.

After cleanup, remove ONLY the temporary cleanup statements previously added
to that runtime policy and prove the original policy semantics are restored.

Do not remove the existing local-QA signer authority or unrelated statements.

CLEANUP SEQUENCE

Before mutation, re-enumerate the exact QA inventory.

Then:

1. enumerate all versions/delete markers for the three exact ready prefixes;
2. remove every matching current object/version/delete marker;
3. prove zero matching S3 state remains;
4. only after AWS cleanup succeeds, remove the exact disposable local database
   fixture in dependency-safe order;
5. prove the 30 posts, 31 relationships, 3 assets and 3 variants are gone;
6. prove pre-existing governed Community/media state is unchanged;
7. remove/revoke any temporary IAM mechanism used;
8. remove the temporary runtime-policy residue described above;
9. verify final IAM state.

Do not perform partial DB cleanup while matching AWS objects remain.

MEDIA ACCEPTANCE

Director visual/product PASS is final.

Carry forward all PROVEN Media seams. Do not rerun or redesign Media unless
cleanup changes an owner, which is not expected.

Canonical Lesson Plans href remains /lessons/. Local reachability is not an
acceptance requirement.

FINALIZE

After verified cleanup:

- inspect final Community and Shared Shell diffs;
- remove superseded Media presentation/navigation experiments;
- preserve unrelated worktree changes;
- update acceptance ledger, Handoff, roadmap and authority;
- commit/consolidate source in each authoritative repository as appropriate;
- push final implementation/evidence;
- issue terminal COMPLETE Report.

FOLLOW-UP INFRASTRUCTURE DEBT

Record, but do not solve inside this objective:

Design a consolidated delegated C3 Media infrastructure-operator model so
routine bounded media IaC/QA operations do not require repeated Director IAM
edits.

The future model should permit broad operation inside the C3 Media namespace
while retaining hard boundaries against unrelated AWS/account resources and
privilege escalation.

NEXT PRODUCT OBJECTIVE

Do not begin it here, but record as preferred next work:

C3 canonical post routing / universal conversation-modal convergence.

Director product direction already established:
- post navigation presents the canonical conversation modal;
- owning Community loads underneath when not already present;
- underlying Community/right rail is dimmed and inert;
- logo, bell and avatar remain available as appropriate;
- no breadcrumb during modal state;
- Create Post is subdued/inert with the underlying page;
- modal reply composer is the contextual engagement CTA;
- no modal stacking;
- reply affordances enter the same modal with reply focus/context.

STOP BOUNDARY

Stop only if exact cleanup requires authority materially outside the C3 Media
namespace, destructive production mutation beyond the three QA media prefixes,
or modification of user credentials/account-level security.

Otherwise carry cleanup and terminal finalization through without another
Director IAM checkpoint.

END TICKET — C3-V1-COMMUNITY-MEDIA-DISCOVERY-IMPLEMENTATION001