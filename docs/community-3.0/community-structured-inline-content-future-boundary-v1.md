# Community Structured Inline Content Future Boundary v1

C3-COMP001 stages attachments and link treatment alongside the ordinary body, preserving text compatibility while leaving room for extension. True inline placement requires a canonical document model, serialization, sanitization, accessibility semantics, migrations, and renderer parity across Thread View and Activity Feed. Those concerns are deferred; compatibility JSON must not silently become an undocumented block format.
